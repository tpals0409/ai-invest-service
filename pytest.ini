[pytest]
pythonpath = .
testpaths = tests
# async 테스트마다 데코레이터를 붙이지 않아도 되게 한다.
# 엔진·적재기 모두 async라 빠뜨리기 쉽고, 빠뜨리면 조용히 건너뛰는 게 아니라 실패한다.
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
